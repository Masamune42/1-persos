{% extends 'base.html.twig' %}

{% block title %}Personnages{% endblock %}

{% block monTitre %}
    Personnage {{ perso.nom }}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-2">
            <img src="{{ asset('images/personnages/' ~ perso.nom ~ '.png') }}">
        </div>
        <div class="col">
            <h2>{{ perso.nom }}</h2>
            <div>
                {% if perso.sexe %}
                    Homme
                {% else %}
                    Femme
                {% endif %}
            </div>
            <h3>
                Les caractéristiques
            </h3>
            <div>
                <ul>
                    <li>{{ perso.carac.force }}</li>
                    <li>{{ perso.carac.agi }}</li>
                    <li>{{ perso.carac.intel }}</li>
                </ul>
            </div>
        </div>
    </div>


{% endblock %}